# OverSeek Environment Configuration
# Quick start:  bash setup.sh  (auto-generates this file)
# Manual start:  cp stack.env.example stack.env  then edit values below

# =============================================================================
# REQUIRED — You must set these (or let setup.sh generate them)
# =============================================================================

# Database password (used on first PostgreSQL init only)
POSTGRES_PASSWORD=changeme_in_production

# Auth token secret (generate with: openssl rand -hex 32)
JWT_SECRET=generate_a_secure_random_string_here

# Encryption key for SMTP passwords, API keys, etc. (openssl rand -hex 32)
# WARNING: Never change after deployment — encrypted data becomes unreadable
ENCRYPTION_KEY=generate_a_secure_random_string_here

# =============================================================================
# DEPLOYMENT — Where your instance lives
# =============================================================================

# Your frontend URL (CORS, OAuth redirects, and API_URL auto-derived from this)
APP_URL=http://localhost:5173

# Application name (shown in emails, 2FA apps, UI)
APP_NAME=OverSeek

# Contact email for push notification VAPID
CONTACT_EMAIL=notifications@localhost

# =============================================================================
# ADVANCED OVERRIDES — Most users can ignore everything below
# =============================================================================
# These are auto-derived from APP_URL at startup. Only set them if you need
# a non-standard setup (e.g., separate API domain, multiple CORS origins).

# API URL (default: api.<your-domain> or localhost:3000)
# API_URL=https://api.example.com

# CORS origins (default: same as APP_URL)
# CORS_ORIGINS=https://example.com,https://app.example.com

# Elasticsearch heap size
# NOTE: These are read by Docker Compose interpolation, NOT env_file.
# To use them, either export on the host or add to a .env file (not stack.env).
# ES_HEAP_MIN=512m
# ES_HEAP_MAX=1g

# Frontend branding (VITE_* — baked into client at build time)
# VITE_APP_NAME=OverSeek
# VITE_SUPPORT_EMAIL=support@yourdomain.com
# VITE_LEGAL_EMAIL=legal@yourdomain.com
# VITE_PRIVACY_EMAIL=privacy@yourdomain.com
